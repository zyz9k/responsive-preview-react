// lib/types.ts
// export interface ScaleConfig {
//   showLabels?: boolean;
//   showScale?: boolean;
// }

export interface PreviewConfig {
  darkMode?: boolean;
  showToolbar?: boolean;
  showScale?: boolean;
  showLabels?: boolean;
}
